# 反无人机行动方案智能生成系统 — 环境配置指南

> 适用对象：合作方技术人员 | 更新时间：2025-06

---

## 一、报错原因分析

我们收到的报错有两类：

| 报错 | 含义 |
|------|------|
| `DLL load failed while importing XXX: 找不到指定的模块` | 系统缺少 **Visual C++ 运行时库**（vcruntime140.dll） |
| `Module use of python312.dll conflicts with this version of Python` | Python 版本不匹配 — `.pyd` 编译于 **Python 3.12**，当前安装的是其他版本 |

**根因：本项目使用 Cython 将核心模块编译为 `.pyd` 动态链接库，编译环境为 Python 3.12 + Windows 64-bit。运行时必须严格匹配。**

---

## 二、环境要求

| 组件 | 版本要求 | 说明 |
|------|----------|------|
| Windows | 10/11 64-bit | 必须 64 位 |
| Python | **3.12.x**（精确） | 3.11 / 3.13 均不可用 |
| Visual C++ Redistributable | VS 2015-2022 (14.40+) | [微软官方下载](https://aka.ms/vs/17/release/vc_redist.x64.exe) |

---

## 三、配置步骤

### 步骤 1：安装 Visual C++ 运行时

下载并安装 [VC++ Redistributable (x64)](https://aka.ms/vs/17/release/vc_redist.x64.exe)：

```
https://aka.ms/vs/17/release/vc_redist.x64.exe
```

安装后**重启终端**（cmd 或 PowerShell）。

### 步骤 2：确认 Python 版本

打开 cmd，执行：

```cmd
python --version
```

**必须输出 `Python 3.12.x`**。如果不是：

- 从 [python.org](https://www.python.org/downloads/) 下载 **Python 3.12.x Windows 64-bit** 安装包
- 安装时勾选 **"Add Python to PATH"**
- 如果同时安装了多个 Python 版本，建议使用 `py -3.12` 启动器，或创建虚拟环境（见下方）

### 步骤 3（推荐）：创建虚拟环境

```cmd
cd C:\WORK\Project\JS-Next\blg\antidrone_delivery
python -m venv venv
venv\Scripts\activate
```

### 步骤 4：安装 Python 依赖

```cmd
pip install -r requirements.txt
```

> **注意**：`torch` 包约 2GB，首次安装需等待 5-15 分钟，请保持网络通畅。

### 步骤 5：配置 API Key

复制并编辑配置文件：

```cmd
copy config.example.json config.json
```

然后用文本编辑器打开 `config.json`，填入你的 API Key：

```json
{
    "provider": "deepseek",
    "api_key": "在此填入你的API-Key",
    "model": "deepseek-chat",
    "base_url": "",
    "temperature": 0.7,
    "max_tokens": 8000
}
```

> 支持的服务商：`deepseek`、`openai`、`qwen`、`zhipu`、`moonshot`、`siliconflow`、`custom`。  
> 如使用自定义 API 端点，设置 `"provider": "custom"` 并填写 `base_url`。

### 步骤 6：验证编译状态

```cmd
python main_compiled.py --check
```

**预期输出**（全部显示 `[OK] ... [已编译]`）：

```
模块编译状态:
--------------------------------------------------
  [OK] scenario_builder               [已编译]
  [OK] evaluator                      [已编译]
  [OK] llm_interface_cloud            [已编译]
  [OK] prompt_templates               [已编译]
  [OK] utils                          [已编译]
--------------------------------------------------
```

如果仍然报 `DLL load failed` 或 `python312.dll conflicts`，请回到步骤 1 和步骤 2 重新确认。

### 步骤 7：启动系统

**交互式命令行模式：**
```cmd
python main_compiled.py
```

**Web 图形界面模式：**
```cmd
python main_compiled.py --web
```

---

## 四、常见问题排查

### Q1：`pip install` 报 `ERROR: Could not find a version that satisfies the requirement torch`

PyTorch 官方源速度较慢，可使用国内镜像：

```cmd
pip install torch -i https://pypi.tuna.tsinghua.edu.cn/simple
```

或者指定 CPU 版本直接下载：

```cmd
pip install torch --index-url https://download.pytorch.org/whl/cpu
```

### Q2：`ModuleNotFoundError: No module named 'xxx'`

确认已执行步骤 3 和步骤 4，并确保在正确的虚拟环境中运行。

### Q3：依然报 `DLL load failed` 应该如何排查？

使用 Dependency Walker 或以下命令检查缺失的 DLL：

```cmd
pip install pefile
python -c "import pefile; pe = pefile.PE('scenario_builder.pyd'); [print(e.name.decode()) for e in pe.DIRECTORY_ENTRY_IMPORT]"
```

常见缺失项：`vcruntime140.dll`、`python312.dll`、`MSVCP140.dll`。

### Q4：是否可以在 Linux 上运行？

不可以。`.pyd` 是 Windows 专用格式。如需 Linux 部署，需重新编译为 `.so` 或提供源码版。

---

## 五、技术支持

如以上步骤未能解决，请提供以下信息以便我们进一步排查：

1. `python --version` 完整输出
2. `pip list` 输出（特别是 torch、openai 版本）
3. `python main_compiled.py --check` 的完整报错截图
4. 操作系统版本（`winver` 命令可查看）
